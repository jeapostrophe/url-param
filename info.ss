(module info (lib "infotab.ss" "setup")
  (define name "URL parameters")
  (define homepage "http://jay.makeoutcity.com/#url-param")
  (define blurb
    (list "Stores Web cells in URLs"))
  (define primary-file "url-param.ss")
  (define doc.txt "doc.txt")
  (define can-be-loaded-with `all)
  (define categories `(net)))